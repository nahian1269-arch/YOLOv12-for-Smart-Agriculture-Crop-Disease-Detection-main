# NuroAgro

Smart agriculture platform for traditional, hydroponic, aquaponic, aeroponic, and hybrid vertical farms.

## Run

```bash
npm start
```

The first run creates `.venv` and installs the pinned Python dependencies. Open `http://127.0.0.1:5000`.

Use `npm run dev` for Flask debug mode or `npm run setup` to refresh Python dependencies.
